

	<div class="list-teal">
		<img class="list-teal-logo" src="/contents/images/logos/tlb.png" alt="logo tlb">
		<h1 class="h1 list-teal-title">Company index</h1>
		<table class="table">
			<thead>
				<tr>
					<th>Id</th>
					<th>Name</th>
					<th>Matricule</th>
					<th>Logo</th>
					<th>Language</th>
					<th>actions</th>
				</tr>
			</thead>
			<tbody>
				{% for key, company in cast_to_array(element_teal) %}
					<tr>

          {{dump(key, company)}}
						{# <td>{{ company.id }}</td>
						<td>{{ company.name }}</td>
						<td>{{ company.matricule }}</td>
						<td>{{ company.logo }}</td>
						<td>{{ company.language }}</td> #}
						<td>
							<a class="btnGlobalSmall" href="{{ path('company_show', {'id': company.id}) }}">show</a>
							<a class="btnGlobalSmall" href="{{ path('company_edit', {'id': company.id}) }}">edit</a>
						</td>
					</tr>
				{% else %}
					<tr>
						<td colspan="6">no records found</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
		<a class="btnGlobal" href="{{ path('company_new') }}">Create new</a>
	</div>

