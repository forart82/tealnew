<!DOCTYPE html>

<title>Edit Result</title>

{% block body %}
    <h1>Edit Result</h1>

    {{ include('result/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('result') }}">back to list</a>

    {{ include('result/_delete_form.html.twig') }}
{% endblock %}
