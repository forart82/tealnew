<!DOCTYPE html>

<title>CsvKeyValues</title>

{% block body %}
    <h1>CsvKeyValues</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ csv_key_value.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ csv_key_value.name }}</td>
            </tr>
            <tr>
                <th>AsValue</th>
                <td>{{ csv_key_value.asValue }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ csv_key_value.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('csv_key_values_index') }}">back to list</a>

    <a href="{{ path('csv_key_values_edit', {'id': csv_key_value.id}) }}">edit</a>

    {{ include('csv_key_values/_delete_form.html.twig') }}
{% endblock %}
